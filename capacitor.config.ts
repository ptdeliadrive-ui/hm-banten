import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hiswanabanten.keuangan',
  appName: 'Hiswana Banten',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
